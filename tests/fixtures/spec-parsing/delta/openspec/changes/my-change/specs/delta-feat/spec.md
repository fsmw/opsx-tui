## ADDED Requirements

### Requirement: Delta feature
A new feature added in this change.

#### Scenario: Delta works
- **WHEN** the delta spec is parsed
- **THEN** it produces a ParsedSpec like any other spec

### Requirement: Override behavior
Changes existing behavior.

#### Scenario: Modified
- **WHEN** the override is applied
- **THEN** the new behavior takes effect

## REMOVED Requirements

### Requirement: Legacy behavior
Removed in this change.

#### Scenario: Removed
- **WHEN** the legacy code path is removed
- **THEN** it no longer exists
